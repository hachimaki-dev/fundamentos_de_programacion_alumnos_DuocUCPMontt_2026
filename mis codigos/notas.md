Para listas, diccionarios, for, while,etc

.append()
.remove()
.len()
.values()
.items()

#buscar ejemplos despues
para filtrar datos puedo usar if

para listas se hace asi:

lista = ["hola", "saludo", "que tal"]
print(lista[0])


diccionario = {"nombre":"Slime", "hp": 45}
print(diccionario["nombre"])

diccionario["hp"] = 54

este ultimo para modificar los datos

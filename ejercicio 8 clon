import random

numero = random.randint(1,50)
ajustado = (numero // 3) * 3
print("Advina el multiplo de 3")

for intento in range(1,4):
    respuesta = int(input(f"Intento {intento}:"))

    if respuesta == ajustado:
        print("¡Correcto!")
        break
    else:
        print("Incorrecto")

else:
    print("Perdiste. El numero era:", ajustado)
